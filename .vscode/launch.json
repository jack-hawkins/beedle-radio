{

  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run App (subdirectory)",
      "program": "${workspaceFolder}/app/server.js",
      "cwd": "${workspaceFolder}/app",
      "console": "integratedTerminal",
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}